GCS:
    dependencyName: GCS
    inputs:
      names: "[\"borat\"]"
      force_destroy: "{\"borat\":true}"
    outputs:
      bucket: BUCKET
      buckets: BUCKETS
      bucketNames: BUCKET_NAMES
      bucketName: BUCKET_NAME
Memorystore:
  dependencyName: Memorystore
  inputs:
    name: borat-redis 
  outputs:
    host: REDISHOST
    port: REDISPORT

