GCS:
    dependencyName: GCS
    inputs:
      location: us-central1
      names: "[\"borat\"]"
      force_destroy: "{\"borat\":true}"
    outputs:
      bucket: BUCKET
      buckets: BUCKETS
      bucketNames: BUCKET_NAMES
      bucketName: BUCKET_NAME
  