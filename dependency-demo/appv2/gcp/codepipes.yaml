GCS:
    dependencyName: GCS
    inputs:
      location: us-central1
      names: "[\"borat\"]"
      force_destroy: "{\"borat\":true}"
    outputs:
      bucket: BUCKET
      buckets: BUCKETS
      bucketNames: BUCKET_NAMES
      bucketName: BUCKET_NAME
Memorystore:
  dependencyName: Memorystore
  inputs:
    name: borat-redis 
    region: us-central1
  outputs:
    host: REDISHOST
    port: REDISPORT

